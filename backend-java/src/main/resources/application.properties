spring.application.name=jobfit
server.port=${PORT:8080}

## Use Supabase / PostgreSQL: override via environment variables in production
# Example SPRING_DATASOURCE_URL: jdbc:postgresql://<host>:5432/<db>?sslmode=require

# Default DB URL for local dev (override via env vars in production)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.hgeadgfewzewgmhhryqt}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Jiyavinchhi123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
# Ensure Hibernate uses the PostgreSQL dialect

# Ensure Hibernate uses the PostgreSQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connection pool settings tuned for Supabase pooler (session pooling)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

python.service.url=${PYTHON_SERVICE_URL:http://127.0.0.1:5001/analyze}
app.free.scan.limit=3

razorpay.key.id=rzp_test_dummy
razorpay.key.secret=dummy_secret
